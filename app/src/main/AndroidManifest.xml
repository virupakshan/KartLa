<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="www.viru.Kart.La">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/kartlalogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kartlalogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="www.viru.Kart.La.MainActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="www.viru.Kart.La.ShopDisplay"
            android:label="@string/title_activity_shop_display" />
        <activity
            android:name="www.viru.Kart.La.Shop_Full_Details"
            android:label="@string/title_activity_shop__full__details" />
        <activity android:name="www.viru.Kart.La.About_us_webdisplay" />
        <activity android:name="www.viru.Kart.La.Home_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="www.viru.Kart.La.Full_Map_View"
            android:label="@string/title_activity_full__map__view" />
        <activity
            android:name="www.viru.Kart.La.Map_Expand_allShops"
            android:label="@string/title_activity_map__expand_all_shops"></activity>
    </application>

</manifest>